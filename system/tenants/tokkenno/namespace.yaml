apiVersion: v1
kind: Namespace
metadata:
  name: tokkenno
  labels:
    # Marks this as an externally-owned tenant sandbox. The NetworkPolicies and
    # RBAC below scope everything to this label / name.
    tenant: tokkenno
    kubernetes.io/metadata.name: tokkenno
