apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: postgre
    keel.sh/policy: force
    keel.sh/match-tag: "true"
    keel.sh/trigger: poll
  annotations:
    keel.sh/pollSchedule: "@every 72h"
  name: postgre
  namespace: immich
spec:
  strategy:
    type: Recreate
  replicas: 1
  selector:
    matchLabels:
      app: postgre
  template:
    metadata:
      labels:
        app: postgre
    spec:
      containers:
        - image: ghcr.io/immich-app/postgres:14-vectorchord0.4.3
          securityContext:
            runAsUser: 950
          name: postgre
          env:
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: immich
                  key: dbPassword
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: immich
                  key: dbUser
            - name: POSTGRES_DB
              value: immich
            - name: POSTGRES_INITDB_ARGS
              value: "--data-checksums"
            - name: DB_STORAGE_TYPE
              value: "SSD"
            - name: PG_DATA
              value: /var/lib/postgresql/data
          ports:
            - containerPort: 5432
              name: postgre
              protocol: TCP
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
          resources:
            requests:
              # Measured 2026-09-01: 8m CPU / 104Mi at rest. Same reason as the
              # server: 5m/30Mi is not a request, it is a lie to the scheduler.
              cpu: 50m
              memory: 256Mi
      securityContext:
        fsGroup: 950
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: immich-db-data-local
      # The data is a local-path volume, so it exists on exactly one node and the
      # pod has to follow it. The label says what the node gives us -- a local
      # disk this database can live on -- instead of naming the box. nas00 has
      # no business carrying it either way: it is 2 cores / 3.4Gi and flaps under
      # load (see server.yaml for the day it took the CSI drivers down with it).
      nodeSelector:
        storage/local-db: "true"

---
kind: Service
apiVersion: v1
metadata:
  name: postgre
  namespace: immich
spec:
  type: NodePort

  selector:
    app: postgre 

  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432
      name: postgre

---
# Rollback for the 2026-09-02 storage class migration: holds the data as it was
# before the cutover. Nothing mounts it. Kept declared on purpose, because ArgoCD
# prunes what leaves git. Delete this block once the -lan volume has proven itself.
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: immich-db-data
  namespace: immich
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: truenas-iscsi-ssd
  resources:
    requests:
      storage: 5Gi

---
# Rollback for the 2026-09-23 move to local-path, and nothing else: it holds the
# cluster exactly as it was shut down. Taking the LAN path instead of the
# tailscale one bought less than it looked like -- the volume was still an ext4
# journal on a network block device, and orange-pi5's livelock on 2026-09-03
# corrupted it anyway. To roll back, point claimName here again.
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: immich-db-data-lan
  namespace: immich
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: truenas-iscsi-ssd-lan
  resources:
    requests:
      storage: 5Gi

---
# The live database, on the local disk of the node labelled storage/local-db.
# No iSCSI, no NFS, no portal to stall: the only way this volume goes away is
# with the node itself. That trades the daily VolumeSnapshot for a pg_dump,
# which postgres-backup.yaml takes over -- local-path has no snapshot support.
#
# 10Gi is a label, not a limit. local-path is a bind mount off the node's
# filesystem and enforces nothing; the database measured 497Mi at the move.
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: immich-db-data-local
  namespace: immich
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 10Gi
