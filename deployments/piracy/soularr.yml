apiVersion: apps/v1
kind: Deployment
metadata:
    name: soularr-deployment
    namespace: piracy
    labels:
        app: soularr
spec:
    replicas: 0
    strategy:
        type: Recreate
    selector:
        matchLabels:
            app: soularr
    template:
        metadata:
            labels:
                app: soularr
        spec:
            securityContext:
                runAsUser: 950
                runAsGroup: 950
                fsGroup: 950
            containers:
                - name: soularr
                  image: mrusse08/soularr:latest
                  imagePullPolicy: Always
                  env:
                      - name: TZ
                        value: Europe/Madrid
                      - name: SCRIPT_INTERVAL
                        value: '300'
                      - name: SLSKD_API_KEY
                        valueFrom:
                          secretKeyRef:
                            name: slskd
                            key: api-key
                  volumeMounts:
                      - name: data
                        mountPath: /data
                      - name: downloads
                        mountPath: /downloads
                  resources:
                      requests:
                          memory: 64Mi
                          cpu: 50m
                      limits:
                          memory: 512Mi
                          cpu: 200m
            volumes:
                - name: data
                  persistentVolumeClaim:
                      claimName: soularr-config-nfs
                - name: downloads
                  nfs:
                      server: 192.168.0.29
                      path: /mnt/SSD/torrents
            nodeSelector:
              svccontroller.k3s.cattle.io/lbpool: lamg
---
# Moved off truenas-iscsi-ssd-lan on 2026-09-24. This volume is a config.ini,
# a failed-imports JSON and four rotated logs: 8 files, half a megabyte. It
# never needed a block device. The old claim and its statically bound PV stay
# declared until the copy is verified, then both go.
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: soularr-config-nfs
  namespace: piracy
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: truenas-nfs-ssd-lan
  resources:
    requests:
      storage: 1Gi
