apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: enshrouded
    keel.sh/policy: force
    keel.sh/match-tag: "true"
    keel.sh/trigger: poll
  annotations:
    keel.sh/pollSchedule: "@every 72h"
  name: enshrouded
  namespace: games
spec:
  strategy:
    type: Recreate
  replicas: 0
  selector:
    matchLabels:
      app: enshrouded
  template:
    metadata:
      labels:
        app: enshrouded
    spec:
      containers:
        - image: docker.io/sknnr/enshrouded-dedicated-server:latest
          name: enshrouded
          env:
            - name: SERVER_NAME
              value: "DivertiJeep"
            - name: SERVER_PASSWORD
              value: "lovamosviendo"
            - name: PORT
              value: "15637"
            - name: SERVER_SLOTS
              value: "16"
          ports:
            - containerPort: 15637
              name: query-port
              protocol: UDP
          volumeMounts:
            - mountPath: /home/steam/enshrouded/savegame
              name: enshrouded-data
          resources:
            requests:
              cpu: 5m
              memory: 30Mi
      volumes:
        - name: enshrouded-data
          persistentVolumeClaim:
            claimName: enshrouded-data-nfs
      nodeSelector:
        kubernetes.io/arch: amd64
        svccontroller.k3s.cattle.io/lbpool: lamg
---
kind: Service
apiVersion: v1
metadata:
  name: enshrouded
  namespace: games
spec:
  type: LoadBalancer

  selector:
    app: enshrouded

  ports:
    - port: 15637
      targetPort: 15637
      name: query-port
      protocol: UDP
---
# enshrouded is pinned to the home pool, so it takes the LAN path to nas02.
#
# Moved off truenas-iscsi-ssd on 2026-09-24. That class is block storage reached
# over the WAN and it has already destroyed three filesystems in this cluster;
# when the link drops an NFS mount blocks and comes back instead. The old volume
# is kept declared until the copy is verified, then removed.
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: enshrouded-data-nfs
  namespace: games
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: truenas-nfs-ssd-lan
  resources:
    requests:
      storage: 10Gi
